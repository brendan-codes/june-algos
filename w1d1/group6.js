class SLL {
    constructor() {
        this.head = null;
    }

    add(node){
        mySLL.head = this.node


    }

    checkHead(){
        // this.head
        // x nodeHead
        return( mySLL.head == this.head)
    }
}

class Node {
    constructor(data) {
        this.data = data;
        this.next = null;
    }
}

var mySLL = new SLL();
add()
checkHead()



// if a SLL head points a null?
// it's empty! no nodes

// if a Node inside of an SLL points to null?
// it's the last node!
